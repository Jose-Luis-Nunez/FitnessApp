import SwiftUI

struct ActiveSetView: View {
    var body: some View {
        Rectangle()
            .fill(AppStyle.Color.yellow)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
