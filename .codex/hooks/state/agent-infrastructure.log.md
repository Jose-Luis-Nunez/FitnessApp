# Learning Log

Agent learnings are appended here after each self-improvement cycle.

| Date | What was learned | File updated |
|---|---|---|
| 2026-04-13 | Duplicated logic across files drifts out of sync (post-change-validator had stale checklist copy). Skills must reference a single source of truth, never copy. Agents layer eliminated — system is now Rules (enforce) + Skills (execute) only. | `reviewing-agent-infrastructure/SKILL.md` (new "Structural Principles" section) |
| 2026-04-13 | Reference Integrity check only grepped for deleted file names, missed stale concept names in prose text (descriptions, examples). When a whole layer/folder is deleted, also search for the concept name itself. | `reviewing-agent-infrastructure/SKILL.md` (Sektion 1: Reference Integrity erweitert) |
| 2026-04-19 | Hook H6 (`agent-infrastructure.sh`) and Hook H2 (`architecture-sync.sh`) overlap on `architecture-documentation.md`: H6 includes the file (only `state/` and `plans/` are excluded from `.cursor/`), even though H2 + Rule R3 + pre-commit Check 5 already cover doc-sync. Result: every doc-only sync forces a full Verifier-subagent run. Identified during enforcement audit of "rename `LisaScenarioRegressionTests`" task. **Decision deferred** by user — no infra change applied. Candidate fix on next revisit: exclude `^.cursor/references/architecture-documentation\.md$` from H6's `EXCLUDE_RE`. | none yet (logged for future reflection) |
| 2026-04-19 | Pure-rename / comment-only Swift edits (no logic change) hit the full `reviewing-code-changes` skill checklist via `code-changes-enforcement.mdc`. Skip clause currently allows only "pure AppStyle token swaps". User explicitly chose to keep the clause narrow. Logged here so we don't re-litigate; treat full-skill-run as the cost of keeping the rule deterministic. | none (intentional, per user) |
| 2026-04-22 | The `§2 Reuse Opportunities` table is keyed by named components. Structural duplication patterns with no single canonical target (e.g. `Title + Picker(.wheel) + .clipped()` column; `CapsuleToggleStyle + Text("Decimal")` pair; inline `for i in … { options.append(…) }` weight-options loop) slipped through: 5+ inline copies across `ExercisePickerView`, `ExerciseWeightPickerView`, `ActiveSetEditPickerView`, `AddAnalyticsEntryView`, `ProfileView`. Agent self-approved `Reuse — PASS` on stamp. Root cause: table-only check matches on keywords, not shapes. **Fix:** added 3 rows for the structural patterns + a "Beyond-the-table duplication check" subsection with 3 grep queries the agent must run. Also mandated `## Residual Duplications` section in every `code-changes.stamp.md` so knowingly-unfixed duplication is a first-class artifact, not hidden debt. | `reviewing-code-changes/SKILL.md` §2 + Output section |
| 2026-05-01 | T8d migration revealed two enforcement gaps. (1) `build-and-test.mdc` rule listed `xcodebuild test -scheme FitnessApp -only-testing:FitnessAppUITests` as the UI-test command, but only the `FitnessApp UITests` scheme builds with the `UITesting` configuration that defines `UITESTING`. Running the wrong scheme silently skips `UITestLaunchStrategy` (the entire `--uitesting` path is `#if`'d out) and the test fails with seemingly unrelated selector errors. Cost: ~1h debugging a *working* migration. (2) Reference Integrity check on `.cursor/` files initially scanned only file names + the directly-edited files; the verifier subagent caught two additional stale type references (`InactiveCardView` / `ActiveCardView` / `IdleActiveCardView`) buried in unrelated paragraphs of `architecture-documentation.md` (FitnessProfile BMI-card animation comparison; FeedbackSheetComponent shared-component see-also). Lesson: when deleting feature types that have visible names, project-wide rg across `.cursor/references/` is mandatory, not just the obvious doc sections. The Verifier subagent caught it independently (defense-in-depth working as intended). | `build-and-test.mdc` (UI-tests scheme guidance + warning prose); `architecture-documentation.md` (3 stale-type fixes + T8c prose correction); `ui-test-conventions.md` (4 stale-type fixes + Test ID Enums table restructure); `reviewing-code-changes/SKILL.md` §13h (historical-note softening) |
| 2026-05-01 | Two-skill split (`writing-ui-tests` / `updating-ui-tests`) had no home for the **debugging** workflow. User feedback after the T8d selector failure surfaced a missing diagnostic discipline: the agent jumped straight to "adjust data pipeline" or "raise timeout" without first checking (a) the use-case flow + selector sequence and (b) whether the expected selector was present in the AX-tree at all. The fix is a third UI-test skill `debugging-ui-tests` plus a shared 5-step decision tree (`Diagnosing a Failing Selector`) in `ui-test-conventions.md` — same DRY pattern as the existing Review Checklist / DSL Reference. Verifier subagent flagged a first FAIL (handoff backreferences missing in callee skills + incomplete overview row), demonstrating again that handoff bidirectionality (per `reviewing-agent-infrastructure` § Handoff Links) is easy to forget when adding a new skill mid-cycle. Disambiguated `updating-ui-tests` description (`broken,` removed) so the keyword matcher reliably routes "my test is broken" to `debugging-ui-tests`. | `ui-test-conventions.md` (new `## Diagnosing a Failing Selector` section + `### Common Pitfalls`); `.cursor/skills/debugging-ui-tests/SKILL.md` (new); `writing-ui-tests/SKILL.md` (1-sentence pointer in Step 2c + sync-table row); `updating-ui-tests/SKILL.md` (description disambiguated, 1-sentence pointer in Step 1, sync-table row); `reviewing-agent-effectiveness/SKILL.md` + `reviewing-agent-infrastructure/SKILL.md` (handoff backreferences in "When to Use" / "When to Activate"); `agent-system-overview.md` § L3 (new row + cross-reference enrichment) |
